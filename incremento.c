#include <stdio.h>

int main() {
  // Declaração de variáveis
  int contador = 1;

  // Loop de incremento
  while (contador <= 10) {
    // Saída de dados
    printf("O contador é: %d\n", contador);

    // Incremento do contador
    contador++;
  }

  return 0;
}
